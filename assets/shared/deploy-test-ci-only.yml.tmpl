name: __WORKFLOW_NAME__

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: __CONCURRENCY_GROUP__
  cancel-in-progress: true

jobs:
  guidance:
    runs-on: __RUNNER__
    timeout-minutes: __DEPLOY_TIMEOUT_MINUTES__
    environment: __TEST_ENVIRONMENT__
    defaults:
      run:
        shell: "__DEFAULT_SHELL__"
    steps:
__DEPENDENCY_REMINDER_STEP__
      - name: Explain next step
        run: |
          echo "CI-only mode is enabled for __APP_NAME__."
          echo "No automated test deployment target is configured yet."
          echo "Choose a deployment strategy, then replace this workflow."
