name: __WORKFLOW_NAME__

on:
  workflow_dispatch:
  push:
    branches:
__TEST_BRANCH_LINES__

permissions:
  contents: read
  packages: write

concurrency:
  group: __CONCURRENCY_GROUP__
  cancel-in-progress: true

jobs:
  publish-test-image:
    runs-on: __RUNNER__
    timeout-minutes: 30
    environment: __TEST_ENVIRONMENT__
    env:
      IMAGE_NAME: __IMAGE_REGISTRY__/__APP_NAME__
      IMAGE_TAG: test-${{ github.sha }}
      IMAGE_BRANCH_TAG: test-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: __IMAGE_REGISTRY_HOST__
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push test image
        uses: docker/build-push-action@v6
        with:
          context: __DOCKER_CONTEXT__
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
            ${{ env.IMAGE_NAME }}:${{ env.IMAGE_BRANCH_TAG }}
