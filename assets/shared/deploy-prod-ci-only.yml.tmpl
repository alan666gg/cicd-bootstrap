name: __WORKFLOW_NAME__

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: __CONCURRENCY_GROUP__
  cancel-in-progress: true

jobs:
  guidance:
    runs-on: __RUNNER__
    timeout-minutes: 10
    environment: __PROD_ENVIRONMENT__
    steps:
      - name: Explain next step
        run: |
          echo "CI-only mode is enabled for __APP_NAME__."
          echo "No automated production deployment target is configured yet."
          echo "Choose a deployment strategy, then replace this workflow."
